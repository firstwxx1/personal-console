import * as React from "react";
import { AlertTriangle, RotateCcw, Trash2 } from "lucide-react";
import { Button, Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui";
export function DangerZone({ onReset }: { onReset: () => void }) { const [action, setAction] = React.useState<"clear" | "defaults" | null>(null); const title = action === "clear" ? "清除本地偏好" : "恢复默认设置"; function confirm() { onReset(); setAction(null); } return <><Card className="border-danger/35"><CardHeader><CardTitle className="flex items-center gap-2 text-danger"><AlertTriangle className="h-4 w-4" aria-hidden="true" />危险区域</CardTitle><CardDescription>以下操作只会重置本组件的界面状态，不会删除文件、数据或调用外部 API。</CardDescription></CardHeader><CardContent className="flex flex-col gap-2 sm:flex-row"><Button variant="destructive" size="sm" onClick={() => setAction("clear")}><Trash2 className="h-3.5 w-3.5" aria-hidden="true" />清除本地偏好</Button><Button variant="outline" size="sm" onClick={() => setAction("defaults")}><RotateCcw className="h-3.5 w-3.5" aria-hidden="true" />恢复默认设置</Button></CardContent></Card>{action && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4" role="presentation"><div role="dialog" aria-modal="true" aria-labelledby="danger-dialog-title" className="w-full max-w-sm rounded-md border border-border bg-card p-5 shadow-overlay"><h2 id="danger-dialog-title" className="text-base font-semibold">{title}？</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">确认后仅重置当前页面的临时设置，其他页面与本地文件不会受到影响。</p><div className="mt-4 flex justify-end gap-2"><Button variant="ghost" size="sm" onClick={() => setAction(null)}>取消</Button><Button variant="destructive" size="sm" onClick={confirm}>确认</Button></div></div></div>}</>; }
